# Local Admin

The public admin route lives in `app/admin` and uses `components/admin`.

This folder is reserved for local administration notes, JSON editing workflows, and future helper scripts that remain compatible with static GitHub Pages deployment.
